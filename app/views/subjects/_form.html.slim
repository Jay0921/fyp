.card
  .card-header
    h3.card-title New Subject
  = simple_form_for [subject] do |f|
    .card-body
      .form-group
        = f.input :name, input_html: { class: "form-control" }
    .card-footer
      = f.button :submit, class: "btn btn-success"